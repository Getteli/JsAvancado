/**
 * Usando a propria classe do Objeto, que usamos para seal, freeze e preventExtensions
 * ele possui metodos que podemos usar para checar se estão ou nao. usando seus verbos e is no inicio.
 * ex:
 * isSealed (se está selado)
 * isExtesible (se é extensivel)
 * isFrozen (se está congelado)
 * Você passa o objeto como parametro e ele fará a checagem
 */